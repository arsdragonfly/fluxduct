# WIP

Fluxduct aims to be the best pipewire patchbay.

## Build

Requires Node.js 22 and Rust. Make sure `$HOME/.cargo/bin` is in `PATH`.

```bash
cargo install typeshare-cli
npm i
# TODO: build fluxduct-rs
npm run start
```
